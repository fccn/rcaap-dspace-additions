Assunto: Repositório - A exportação do registo não foi finalizada.
A exportação solicitada no repositório não foi finalizada devido a:

 ${params[0]}

Para mais informação, contacte o administrador:

 ${params[1]}

A Equipa do Repositório ${config.get('dspace.name')}.

