## Email sent to DSpace users when their batch import fails.
##
## Parameters: {0} the export error
##             {1} the URL to the feedback page
##
##
## See org.dspace.core.Email for information on the format of this file.
##
#set($subject = 'DSpace - The batch import was not completed.')
A importação em lote iniciada na interface DSpace não foi terminada, devido à seguinte razão:
 ${params[0]}

Para mais informações deverá contactat o seu administrador de sistema:
 ${params[1]}


A Equipa do Repositório ${config.get('dspace.name')}.

