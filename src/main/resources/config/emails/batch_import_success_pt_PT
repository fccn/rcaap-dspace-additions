A importação em lote iniciada na interface DSpace terminou com sucesso.

Poderá encontrar o mapfile da importação no seguinte caminho: ${params[0]}


A Equipa do Repositório ${config.get('dspace.name')}.