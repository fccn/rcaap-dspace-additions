## Email enviado aos utilizadores do DSpace quando se esquecem da sua palavra-passe.
##
## Parâmetros: {0} é expandido para um URL especial
##
## Consulte org.dspace.core.Email para obter informações sobre o formato deste ficheiro.
##
#set($subject = "${config.get('dspace.name')}: Pedido de alteração de palavra-passe")
#set($phone = ${config.get('mail.message.helpdesk.telephone')})

Para alterar a palavra-passe da sua conta no ${config.get('dspace.name')}, por favor clique na seguinte ligação:

  ${params[0]}

Se precisar de assistência com a sua conta, envie um email para ${config.get("mail.helpdesk")}
#if( $phone )
ou contacte-nos através do telefone ${phone}.
#end

A Equipa do ${config.get('dspace.name')}