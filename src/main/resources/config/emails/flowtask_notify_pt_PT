Assunto: Repositório: Relatório da tarefa de curadoria

Título:          ${params[0]}
Coleção:     ${params[1]}
Submetido por:   ${params[2]}

Tarefa de curadoria  ${params[3]} foi realizada
com o seguinte resultado:

 ${params[4]}

Ação feita na submissão:  ${params[5]}

A Equipa do Repositório ${config.get('dspace.name')}.
