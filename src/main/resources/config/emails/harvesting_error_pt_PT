Assunto: Repositório: Erro de Recolha
Coleção ${params[0]} falhou na recolha:

Data:       	${params[1]}
Valor: 	${params[2]}

${params[3]}

Exceção:
${params[4]}

A Equipa do Repositório ${config.get('dspace.name')}.